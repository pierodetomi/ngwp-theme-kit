import { ThemeSettingDirective } from './theme-setting.directive';

describe('ThemeSettingDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemeSettingDirective();
    expect(directive).toBeTruthy();
  });
});
