import { WidgetAreaDirective } from './widget-area.directive';

describe('WidgetAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new WidgetAreaDirective();
    expect(directive).toBeTruthy();
  });
});
